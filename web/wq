export default function Home() {
  return (
    <main className="min-h-screen bg-slate-950 text-white">
      <section className="mx-auto flex min-h-screen max-w-6xl flex-col items-center justify-center px-6 text-center">
        <p className="mb-4 rounded-full border border-cyan-400/30 px-4 py-2 text-sm text-cyan-300">
          Stellar Soroban Escrow Smart Contract
        </p>

        <h1 className="max-w-4xl text-4xl font-bold tracking-tight md:text-6xl">
          Secure service payments between clients and specialists.
        </h1>

        <p className="mt-6 max-w-2xl text-lg text-slate-300">
          Soroban Service Escrow protects both clients and service providers by
          locking payment in a smart contract until work is completed and approved.
        </p>

        <div className="mt-10 flex flex-col gap-4 sm:flex-row">
          <button className="rounded-xl bg-cyan-400 px-6 py-3 font-semibold text-slate-950 hover:bg-cyan-300">
            Connect Wallet
          </button>

          <button className="rounded-xl border border-slate-600 px-6 py-3 font-semibold text-white hover:bg-slate-800">
            View Documentation
          </button>
        </div>

        <div className="mt-16 grid w-full gap-6 md:grid-cols-3">
          <div className="rounded-2xl border border-slate-800 bg-slate-900 p-6 text-left">
            <h2 className="text-xl font-semibold">Create Job</h2>
            <p className="mt-3 text-slate-400">
              A client creates a service job and assigns a specialist.
            </p>
          </div>

          <div className="rounded-2xl border border-slate-800 bg-slate-900 p-6 text-left">
            <h2 className="text-xl font-semibold">Fund Escrow</h2>
            <p className="mt-3 text-slate-400">
              Payment is locked safely inside a Soroban smart contract.
            </p>
          </div>

          <div className="rounded-2xl border border-slate-800 bg-slate-900 p-6 text-left">
            <h2 className="text-xl font-semibold">Release Payment</h2>
            <p className="mt-3 text-slate-400">
              Funds are released only after the client approves the completed work.
            </p>
          </div>
        </div>
      </section>
    </main>
  );
}
